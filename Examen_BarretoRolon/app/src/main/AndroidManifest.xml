<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itiudc.examen_barretorolon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Examen_BarretoRolon">
        <activity
            android:name=".activities.ResultsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.ThirdQuestion"
            android:exported="true" />
        <activity
            android:name=".activities.SecondQuestion"
            android:exported="true" />
        <activity
            android:name=".activities.FirstQuestion"
            android:exported="true" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>